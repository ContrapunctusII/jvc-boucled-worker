/**
 * Interface représentant l'ensemble des paramètres de configuration du worker.
 * 
 * @interface
 * @name Config
 * @kind interface
 */
interface Config {
    proxyURL: string | null;
}

export default Config;